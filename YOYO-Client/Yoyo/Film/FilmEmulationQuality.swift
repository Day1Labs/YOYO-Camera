/// Effect processing quality level
public enum FilmEmulationQuality {
    /// Preview mode - fast approximation, guaranteed frame rate
    case preview
    /// Full mode - full physics simulation, highest quality
    case full
}
